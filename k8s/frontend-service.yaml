apiVersion: v1
kind: Service
metadata:
  name: autos2-frontend
  namespace: autos2
  labels:
    app: autos2-frontend
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: autos2-frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
