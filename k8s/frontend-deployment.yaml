apiVersion: apps/v1
kind: Deployment
metadata:
  name: autos2-frontend
  namespace: autos2
  labels:
    app: autos2-frontend
    tier: frontend
    version: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: autos2-frontend
  template:
    metadata:
      labels:
        app: autos2-frontend
        tier: frontend
        version: prod
    spec:
      nodeSelector:
        kubernetes.io/hostname: thor
      containers:
      - name: frontend
        image: localhost/autos2-frontend:v1.0.3
        imagePullPolicy: Never
        ports:
        - containerPort: 80
          name: http
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"